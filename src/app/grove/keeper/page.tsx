import AdminPage from "@/app/admin/page";

export const metadata = {
  title: "Keeper Gate",
  description: "Secret entrance to the guestbook admin panel.",
};

const KeeperGate = () => <AdminPage />;

export default KeeperGate;

